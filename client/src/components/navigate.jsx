import React from 'react';
import { useNavigate } from 'react-router-dom';

export default function NavigateToAdminDashboard() {
    const navigate = useNavigate();
    
    // Add logic to navigate to "/AdminDashboard" if needed

    return null; // or return some component if required
}